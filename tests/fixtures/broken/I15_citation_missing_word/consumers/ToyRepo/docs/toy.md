# Toy consumer document

## T1. Toy anchor heading

This section relies on MD_0005 and on MD_0006 as established results.

- **T1(a). A toy sub-item.** It carries its own item ID, so the anchor `T1`
  names the heading above and never this line.
