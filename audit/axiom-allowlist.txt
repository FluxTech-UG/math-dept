# Per-declaration axiom exceptions: "<decl> <axiom>" per line.
#
# On Lean v4.33.1 a `native_decide` proof generates a per-declaration axiom
# named after the declaration itself, so an allowlist line is written from
# what `make lean` actually reports, never guessed. Example:
# MathDept.MD_0012.statement MathDept.MD_0012.statement._native.native_decide.ax_1_1
